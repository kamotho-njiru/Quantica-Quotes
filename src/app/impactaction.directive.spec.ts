import { ImpactactionDirective } from './impactaction.directive';

describe('ImpactactionDirective', () => {
  it('should create an instance', () => {
    const directive = new ImpactactionDirective();
    expect(directive).toBeTruthy();
  });
});
